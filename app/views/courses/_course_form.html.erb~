<%= render 'shared/error_messages', :object => object %>	
	<div class="field">
		<%= f.label :experience %><br />
		<%= f.number_field :experience %>
	</div>
	<div class="field">
		<%= object.label 'Course Type' %><br />
		<%= object.text_field :course_type %>
	</div>
	<div class="field">
		<%= object.label :date %><br />
		<%= object.date_select :date %>
	</div>
	<div class="field">
		<%= object.label :time %><br />
		<%= object.time_select :time %>
	</div>
	<div class="field">
		<%= object.label :contact %><br />
		<%= object.text_field :contact %>
	</div>
<table>
<tr> 
	<td>	
		<b>Clients</b>
		<ul class="checkbox-list">
		<% @course.clients.each do |client| %>
			<li><%= check_box_tag "course[client_ids][]", client.id, @course.clients.include?(client) %> <%= client.name %></li>
		<% end %>
	</ul>
	</td>
	<td>
		<b>Guides:</b>
			<ul class="checkbox-list">
			<% @course.guides.each do |guide| %>
				<li><%= check_box_tag "course[guide_ids][]", guide.id, @course.guides.include?(guide) %> <%= guide.name %></li>
			<% end %>
		</ul>
	</td>
</tr>
<tr>

	<td>
			<div class ="field">			
				<b>Add Client:</b><br/>
				<%= object.collection_select(:num_clients, @clients, :id , :name, {:include_blank => 'none', :selected => 'none'} ) %>
			</div>
	</td>	
	
	<td>
			<div class="field">
					<b>Add Guide:</b><br/>
					<%= object.collection_select(:num_guides, @guides, :id , :name, {:include_blank => 'none', :selected => 'none'} ) %>
				</div>

	</td>
</tr>
</table>
